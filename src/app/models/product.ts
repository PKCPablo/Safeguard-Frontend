export class Product {
    id: number;
    nombre: string;
    precio: string;

    constructor(id: number, nombre: string, precio: string) {
        this.id = id;
        this.nombre = nombre;
        this.precio = precio;
    }
}
